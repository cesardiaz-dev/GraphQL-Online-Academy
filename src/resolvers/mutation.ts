const mutation = {
    Mutation: {
        // addCourse: (course: any) => {},
        // updateCourse: (course: any) => {},
        // deleteCourse: (id: string) => {},
    }
};

export default mutation;